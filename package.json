{
  "name": "skyview-weather",
  "version": "1.0.0",
  "description": "SunnyBabe Weather App — Capacitor iOS wrapper",
  "scripts": {
    "sync": "cap sync",
    "ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
